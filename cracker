local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local Players = game:GetService("Players")

local apiUrl = "https://8f352b3f-b533-4a33-b6f4-312ef0eace4d-00-107ppso2ogc0.janeway.replit.dev/getjob?user=global"

local lastJobId = ""
local lastTeleportTime = 0
local debounceTime = 3
local pollInterval = 0.1

print("Auto follower started. Polling every " .. pollInterval .. " seconds")

spawn(function()
    while true do
        local success, response = pcall(function()
            return HttpService:GetAsync(apiUrl)
        end)

        if success then
            local data = HttpService:JSONDecode(response)
            local friendJobId = data.jobId
            local friendPlaceId = data.placeId

            if friendJobId and friendJobId ~= "" and friendJobId ~= lastJobId then
                lastJobId = friendJobId

                local myPlaceId = game.PlaceId
                if friendPlaceId == myPlaceId then
                    local myJobId = game.JobId
                    local now = tick()

                    if myJobId ~= friendJobId and (now - lastTeleportTime) >= debounceTime then
                        print("New jobId detected → teleporting")
                        task.wait(0.5)

                        lastTeleportTime = tick()

                        local teleportSuccess, errorMsg = pcall(function()
                            TeleportService:TeleportToPlaceInstance(friendPlaceId, friendJobId, {Players.LocalPlayer})
                        end)

                        if not teleportSuccess then
                            warn("Teleport failed: " .. tostring(errorMsg))
                        end
                    end
                end
            end
        else
            warn("API request failed: " .. tostring(response))
        end

        task.wait(pollInterval)
    end
end)
